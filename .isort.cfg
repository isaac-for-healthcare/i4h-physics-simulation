[settings]
# Without this file isort falls back to line_length=79 with GRID output, which
# emits backslash continuations and hanging indents that contradict the
# trailing-comma style the codebase is written in (and that ruff/black expect).
profile = black
line_length = 120
